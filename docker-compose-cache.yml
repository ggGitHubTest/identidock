version: '3'
identidock:
  build:
   context: .
   dockerfile: Dockerfile.7
  ports:
   - "5000:5000"
  environment:
   ENV: DEV
  volumes:
   - cache-app:/app
  links:
   - dnmonster
   - redis

dnmonster:
  image: amouat/dnmonster:1.0

redis:
  image: redis:3.0
